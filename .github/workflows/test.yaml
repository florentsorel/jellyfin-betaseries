name: '🧪 Test Plugin'

on:
  push:
    branches:
      - master
      - v1.x
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches:
      - master
      - v1.x
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/test.yaml@fdc7da9b15f696978f5973c1616c8bc663c677e8 # master
